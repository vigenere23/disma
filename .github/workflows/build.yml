name: Build

on:
  push:
    branches:
      - "**"

jobs:
  disma:
    uses: vigenere23/disma/.github/workflows/build.template.yml@master
    with:
      package: disma

  disma-cli:
    needs: disma
    uses: vigenere23/disma/.github/workflows/build.template.yml@master
    with:
      package: disma-cli
