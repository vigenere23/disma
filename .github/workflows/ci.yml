name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Lint
        run: ./ci/lint.sh

      - name: Build
        run: cargo build --verbose

      - name: Test
        run: cargo test --verbose
